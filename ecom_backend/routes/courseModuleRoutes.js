// const express = require('express');
// const { courseModuleController } = require("../controller/index");

// const router = express.Router();

// // once again composite keys are used right now, we can consider having unique ids for every module
// router.get('/courses/:courseId/modules', courseModuleController.getAllModulesForCourse);
// router.get('/courses/:courseId/modules/:moduleId', courseModuleController.getModuleById);
// router.post('/courses/:courseId/modules', courseModuleController.createModule);
// router.put('/courses/:courseId/modules/:moduleId', courseModuleController.updateModule);
// router.delete('/courses/:courseId/modules/:moduleId', courseModuleController.deleteModule);

// module.exports = router;
